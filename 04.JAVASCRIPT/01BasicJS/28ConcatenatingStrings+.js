const myStr = "This is the start. " + "This is the end."; 

/*
Concatenación de cadenas con el operador Plus
En JavaScript, cuando el +operador se usa con un Stringvalor, se llama operador de concatenación . Puede crear una nueva cadena a partir de otras cadenas concatenándolas .

Ejemplo

'My name is Alan,' + ' I concatenate.'
Nota: Cuidado con los espacios. La concatenación no agrega espacios entre cadenas concatenadas, por lo que deberá agregarlos usted mismo.

Ejemplo:

const ourStr = "I come first. " + "I come second.";
La cadena I come first. I come second.se mostraría en la consola.

Compile myStra partir de las cadenas This is the start.y This is the end.use el +operador. Asegúrese de incluir un espacio entre las dos cadenas.
const myStr = ""; // Change this line
*/