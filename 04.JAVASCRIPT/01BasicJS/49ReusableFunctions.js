function reusableFunction(){ //creo función con el nombre indicado y que imprima por consola el mensaje que nos dicen
    console.log("Hi World");
}

reusableFunction();//llamo a la función creada

/*
Escribir JavaScript reutilizable con funciones
En JavaScript, podemos dividir nuestro código en partes reutilizables llamadas funciones .

He aquí un ejemplo de una función:

function functionName() {
    console.log("Hello World");
}
Puede llamar o invocar esta función usando su nombre seguido de paréntesis, así: functionName();Cada vez que se llame a la función, se imprimirá el mensaje Hello Worlden la consola de desarrollo. Todo el código entre llaves se ejecutará cada vez que se llame a la función.

Cree una función llamada reusableFunctionque imprima la cadena Hi Worlden la consola de desarrollo.
Llame a la función.
*/